# Evidence — G0-05 Architecture dependency tests

- Date: `2026-09-08`
- Assignee: `Codex implementation agent`
- Session: `002`
- Status: `In Review / 90%`
- Required review: Engineering Lead + Security

## Outcome

Architecture verification now proves both sides of the guard:

1. the current scaffold passes the dependency rules;
2. allowed fixtures pass for another module's `contracts/public`, an owned DTO, a shared primitive and the approved web telephony wrapper;
3. forbidden fixtures fail the checker for every expected violation, and the wrapper fails if the checker unexpectedly accepts them or omits an expected violation.

## Rules covered by the negative fixtures

| Rule | Fixture | Expected result |
|---|---|---|
| Contract/platform packages must not import NestJS | `tests/architecture/fixtures/forbidden-imports/packages/contracts/src/forbidden-nest-import.ts` | Checker exits `1` and reports the package boundary violation |
| PortSIP browser SDK may only be imported under `apps/web/src/telephony` | `tests/architecture/fixtures/forbidden-imports/apps/web/src/features/agent/forbidden-portsip-import.ts` | Checker exits `1` and reports the SDK boundary violation |
| Module may only consume another module through `contracts/public` | `tests/architecture/fixtures/forbidden-imports/apps/api/src/modules/campaigns/application/forbidden-people-repository-import.ts` | Checker rejects the cross-module repository/internal import |
| Shared/common cannot own ORM/persistence/repository implementation | `tests/architecture/fixtures/forbidden-imports/apps/api/src/shared/persistence/user-orm-entity.ts` | Checker rejects the shared persistence model |
| Domain/application/public contracts cannot import adapter/vendor SDK/DTO | `tests/architecture/fixtures/forbidden-imports/apps/api/src/modules/interactions/domain/forbidden-vendor-dto-import.ts` | Checker rejects the vendor DTO leak |

The intentionally invalid fixture directory is excluded from ESLint's typed-project scan. It remains an input to `scripts/check-architecture-fixtures.mjs`, so the exclusion does not bypass architecture verification.

## Reproduction

Authoritative command:

```powershell
pnpm run test:architecture
```

Observed result:

```text
Architecture dependency checks passed
Architecture allowed and forbidden fixtures passed
```

Full gate:

```powershell
$env:CI='true'
pnpm run check
```

Result: PASS — format, ESLint, TypeScript strict, 2/2 unit tests, architecture source scan, allowed/forbidden fixtures and production build.

## Remaining acceptance

- Engineering Lead and Security must independently review rule coverage and fixture isolation.
- The cross-module/shared-persistence/vendor-leak rules are verified against synthetic path-convention fixtures because the current scaffold does not yet contain those production implementations. Reviewers must confirm the convention before D-010; update checker and fixtures together if the approved module layout changes.
- `G0-05` remains capped at 90% until required reviewers accept the evidence.

Implementation execution re-ran `CI=true; pnpm run check` at commit `75e2880` on `2026-09-09`; source scan and allowed/forbidden fixture checks remained PASS. This is a non-regression check only and does not count as the required separate Security/QA review.

Implementation execution re-ran `CI=true; pnpm run check` at HEAD `dffceb3` on `2026-09-11`; source scan and allowed/forbidden fixture checks remained PASS. This is a non-regression check only and does not count as the required separate Security/QA review.

Reviewer checklist:

- [ ] Run `$env:CI='true'; pnpm run check` from a clean PowerShell checkout (or equivalent syntax for another shell) and retain the output.
- [ ] Confirm another module's `contracts/public` positive fixture remains allowed while its domain/adapters/repository negative fixture remains forbidden.
- [ ] Confirm the shared primitive positive fixture passes while the shared/common persistence negative fixture cannot hide an ORM model or repository.
- [ ] Confirm the owned DTO positive fixture is not rejected merely for using a `dto` path; only adapter/vendor SDK or vendor DTO dependencies are forbidden from core layers.
- [ ] Confirm the negative runner fails if any expected violation disappears.
- [ ] Record reviewer identity, date, commit and PASS/NOT PASS below.

```text
Engineering Lead review:
Security review:
Commit reviewed:
Review date:
Outcome and remaining findings:
```

# Evidence — G0-002 Scaffold verification

- Date: `2026-09-08`
- Assignee: `Codex implementation agent`
- Snapshot: working tree trước initial commit
- Status: `Verification / 80%`

## Kết quả

| Check | Result |
|---|---|
| Dependency install + lockfile | PASS; 10 workspace projects; supply-chain policy pass |
| Format | PASS |
| ESLint | PASS; zero warning |
| TypeScript strict | PASS; 9 projects |
| Unit tests | PASS; 2 files/2 tests |
| Architecture dependency test | PASS |
| Build | PASS; packages/API/worker/web |
| API smoke | PASS; `/health/live` = `ok`, `/health/ready` = `degraded/dependencies-not-configured` |
| PortSIP/Production side effect | Không phát sinh |
| Docker synthetic dependencies | BLOCKED — Docker không có trên host |
| Independent clean-machine review | PENDING — cần reviewer khác implementation agent |

## Artifact

- `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`.
- `apps/web`, `apps/api`, `apps/worker`.
- Contract packages và `scripts/check-architecture.mjs`.
- `docs/BUILD-PROFILE.md`.
- [Independent clean-machine review template](G0-CLEAN-MACHINE-REVIEW.md).

Markdown handover verification: 30 Markdown files, 0 broken relative links, 0 unbalanced code fences.

G0-02 chưa được tính 100% vì chưa có independent reviewer. G0-03 tối đa 90% cho tới khi reviewer khác người viết scaffold hoàn tất [clean-machine record](G0-CLEAN-MACHINE-REVIEW.md).

## Session 002 replay

Ngày `2026-09-08`, cùng host implementation chạy lại toolchain và `CI=true; pnpm run check`: PASS. Architecture verification nay gồm positive scan và [negative fixtures evidence](G0-005.md). Đây là repeatability evidence trên host hiện tại, không thay thế independent clean-machine review.

Một clean checkout local tại commit `5f9130c` đã xác nhận clone, Node `v24.15.0`, pnpm `11.19.0`, commit và frozen lockfile/supply-chain policy trước khi tải package. Install không hoàn tất vì sandbox chặn truy cập `registry.npmjs.org` (`EACCES`/`fetch failed`); yêu cầu escalated registry access bị từ chối do chưa có user authorization rõ cho network/package execution. Không ghi đây là Build Profile failure hoặc clean-machine PASS. Thư mục replay tạm đã được xóa sau khi xác minh đúng target; source working tree không bị ảnh hưởng.

Next action: named independent reviewer chạy [clean-machine record](G0-CLEAN-MACHINE-REVIEW.md) trong môi trường có approved package-registry policy, hoặc Project Owner/user phê duyệt rõ registry access cho một same-host replay bổ sung. Same-host result vẫn không thay chữ ký độc lập.

Ngày `2026-09-09`, Project Owner/user đã phê duyệt registry access. Frozen install tại clean checkout commit `9244e56` thành công, nhưng `format:check` phát hiện 55 file bị checkout thành CRLF dưới Git configuration của execution user. Root cause là repository chưa có `.gitattributes`; working checkout trước đó dùng LF nên không bộc lộ portability defect. Fix candidate thêm `* text=auto eol=lf`; phải chạy lại clean checkout trên commit chứa fix trước khi ghi PASS. Git `safe.directory` warning chỉ do clone và install chạy dưới hai sandbox identities khác nhau; không sửa global Git config.

### Replay sau portability fix

Clean checkout mới tại commit `bf760f6` được tạo với `core.autocrlf=true` để tái tạo điều kiện lỗi trước. Kết quả ngày `2026-09-09`:

| Check | Result |
|---|---|
| Commit | `bf760f64e7ecf277d78f620d4e62eeb5c95a5086` |
| Tracked worktree line endings | PASS — không có `w/crlf`; `.gitattributes` giữ text ở LF |
| `pnpm install --frozen-lockfile` | PASS — 261 packages; supply-chain policy PASS; lockfile unchanged |
| Working tree sau install | Clean |
| Format/ESLint/TypeScript | PASS |
| Unit tests | PASS — 2/2 |
| Architecture source + allowed/forbidden fixtures | PASS |
| Packages/API/worker/web build | PASS |
| Working tree sau full gate | Clean |
| Cleanup | PASS — temporary replay directory removed |

Kết luận: same-host clean-checkout replay PASS và portability defect đã được sửa. Đây là repeatability evidence do implementation agent thực hiện, không phải independent clean-machine sign-off; G0-02 vẫn 80% và G0-03 vẫn tối đa 90% cho tới reviewer độc lập.

### Verification trên HEAD hiện tại

Ngày `2026-09-09`, implementation execution chạy lại `CI=true; pnpm run check` tại commit `75e2880` sau khi đồng bộ role/gate documentation. Kết quả: Prettier, ESLint, TypeScript strict, 2/2 unit tests, architecture source scan, allowed/forbidden fixtures và packages/API/worker/web build đều PASS. Đây là xác nhận không hồi quy trên working tree hiện tại; không thay thế clean-machine hoặc independent reviewer sign-off.

### Clean-checkout replay trên HEAD `ca0e468`

Ngày `2026-09-09`, clean checkout với `core.autocrlf=true` đã chạy lại toàn bộ Build Profile sau khi user ủy quyền terminal/registry access:

| Check | Result |
|---|---|
| Node/pnpm/Git | PASS — `v24.15.0` / `11.19.0` / `2.53.0` |
| Frozen install | PASS — 261 packages; lockfile unchanged |
| Format/ESLint/TypeScript | PASS |
| Unit tests | PASS — 2/2 |
| Architecture source + allowed/forbidden fixtures | PASS |
| Packages/API/worker/web build | PASS |
| Replay worktree | PASS — clean |
| Cleanup | PASS — temporary replay removed |

Kết quả này củng cố repeatability cho `G0-02/G0-03`; vẫn là implementation replay, chưa phải independent reviewer sign-off.

# Local synthetic environment

Chỉ dùng synthetic/anonymized data. Docker Desktop daemon đã được verify; Compose scaffold và repository-owned fake OIDC/PortSIP/CRM đã được tạo nhưng chưa khởi động. Trước khi chạy, phải có digest lock, network isolation và reviewer evidence theo G0-06. Không thêm route hoặc secret Production.

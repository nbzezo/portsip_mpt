# Local synthetic environment

Chỉ dùng synthetic/anonymized data. Docker Desktop daemon đã được verify; PostgreSQL, Redis, fake OIDC, fake PortSIP và fake CRM vẫn chưa được dựng. Trước khi chạy, phải có image/checksum policy, network isolation và reviewer evidence theo G0-06. Không thêm route hoặc secret Production.

# Local synthetic environment

Chỉ dùng synthetic/anonymized data. Docker Desktop daemon đã được verify; Compose stack và repository-owned fake OIDC/PortSIP/CRM đã được replay local-only. Digest lock đã được pin; reviewer evidence đang chờ Sentinel/Beacon sign-off. Không thêm route hoặc secret Production.

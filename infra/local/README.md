# Local synthetic environment

Chỉ dùng synthetic/anonymized data. Máy tạo scaffold chưa có Docker, vì vậy PostgreSQL, Redis, fake OIDC, fake PortSIP và fake CRM chưa được dựng trong phiên này. Không thêm route hoặc secret Production.

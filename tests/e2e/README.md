# E2E tests

Playwright `1.63.0` đã được khóa nhưng browser binary chưa được tải trong Gate 0. G0-06 phải cung cấp synthetic environment trước khi thêm test gọi API/UI; không dùng credential hoặc số điện thoại thật.
